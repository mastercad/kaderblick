<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Neue Videos verfügbar – Kaderblick</title>
    <link rel="stylesheet" href="/css/email.css">
</head>
<body>
    <div class="email-container">
        <div class="email-header" style="background: linear-gradient(135deg, #018606 0%, #015504 100%); color: #fff; border-radius: 8px 8px 0 0; padding: 20px; text-align: center;">
            <h1 style="margin: 0; font-size: 24px; font-weight: 600; letter-spacing: 0.5px;">Neue Videos verfügbar</h1>
        </div>
        <div class="email-content" style="background: #fff; padding: 30px; border-radius: 0 0 8px 8px; box-shadow: 0 4px 6px rgba(16,185,129,0.08);">
            <p style="margin-top:0; color: #059669; font-weight: 600;">Hallo,</p>
            <p style="color: #475569;">es wurden neue Videos in der Kaderblick-Mediathek hochgeladen.</p>
            {% if files|length > 0 %}
                <div style="background: #f1f5f9; border-radius: 6px; padding: 15px; margin: 15px 0;">
                    <h3 style="color: #10b981; font-size: 18px; margin-bottom: 8px;">📹 Hochgeladene Videos:</h3>
                    <ul style="color: #475569;">
                        {% for file in files %}
                            <li style="margin-bottom: 8px;"><a href="{{ file.url }}" style="color: #10b981; text-decoration: none; font-weight: 500;">{% if file.name %}{{ file.name }}{% else %}Unbekannter Dateiname{% endif %}</a></li>
                        {% endfor %}
                    </ul>
                </div>
            {% endif %}
            <div style="text-align: center; margin: 32px 0;">
                <a href="{{ folderUrl }}" style="display: inline-block; padding: 12px 24px; background: linear-gradient(135deg, #018606 0%, #015504 100%); color: #fff; border-radius: 6px; font-weight: 600; text-decoration: none; font-size: 16px; letter-spacing: 0.5px; box-shadow: 0 2px 8px rgba(16,185,129,0.10); transition: background 0.2s;">Zum Ordner</a>
            </div>
            <p style="color: #059669; font-weight: 500;">Sportliche Grüße<br>Dein Kaderblick-Team</p>
        </div>
        <div class="email-footer" style="text-align: center; padding: 20px; font-size: 12px; color: #64748b; border-top: 1px solid #e2e8f0; margin-top: 20px;">
            <p style="margin: 0 0 4px 0;">Diese E-Mail wurde automatisch generiert. Bitte antworte nicht darauf.</p>
            <p style="margin: 0;">&copy; {{ "now"|date("Y") }} Kaderblick. Alle Rechte vorbehalten.</p>
        </div>
    </div>
</body>
</html>
        <div class="email-footer">
            <p>Diese E-Mail wurde automatisch generiert. Bitte antworte nicht darauf.</p>
            <p>&copy; {{ "now"|date("Y") }} Kaderblick. Alle Rechte vorbehalten.</p>
        </div>
    </div>
</body>
</html>
